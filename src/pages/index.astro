---
import BaseLayout from '../layouts/BaseLayout.astro';
import * as homeMd from '../../content/home.md';
import * as highlightsMd from '../../content/highlights.md';
import * as faqMd from '../../content/faq.md';
import Hero from '../components/Hero.astro';
import HighlightCards from '../components/HighlightCards.astro';
import PropertyDetails from '../components/PropertyDetails.astro';

import FAQ from '../components/FAQ.astro';
import MapEmbed from '../components/MapEmbed.astro';
import Floorplans from '../components/Floorplans.astro';
import Gallery from '../components/Gallery.astro';
import Panos from '../components/Panos.astro';
import VirtualTours from '../components/VirtualTours.astro';
import RoomGalleries from '../components/RoomGalleries.astro';
import PropertyFeatures from '../components/PropertyFeatures.astro';
import PropertyDocs from '../components/PropertyDocs.astro';
import BeforeAfterVideo from '../components/BeforeAfterVideo.astro';
import LocationExtras from '../components/LocationExtras.astro';
import site from '../../site.config';
const pageTitle = homeMd?.frontmatter?.title ?? site.title;
const pageDescription = homeMd?.frontmatter?.description ?? site.description;
const coreHighlights = [
  { label: 'Guide Price', value: site.price },
  { label: 'Bedrooms', value: String(site.bedrooms) },
  { label: 'Bathrooms', value: String(site.bathrooms) },
  { label: 'EPC Rating', value: site.epc }
];
const extraHighlights = highlightsMd?.frontmatter?.items || [];
const highlightItems = [...coreHighlights, ...extraHighlights];
---
<BaseLayout title={pageTitle} description={pageDescription}>
  <Hero />
  <section id="about" class="prose mx-auto p-4">
    {homeMd?.Content && <homeMd.Content />}
  </section>
  
  <!-- Main Property Gallery -->
  <Gallery />
  
  <!-- Property Highlights -->
  <HighlightCards items={highlightItems} />
  
  <!-- Virtual 360° Tours - Interactive Experience -->
  <VirtualTours />
  
  <!-- Panoramic Photos - Wide Views -->
  <Panos />
  
  <!-- Room by Room Detailed Galleries -->
  <RoomGalleries />
  
  <!-- Property Details -->
  <PropertyDetails />

  <!-- Special Property Features -->
  <PropertyFeatures />
  
  <!-- Before & After Video -->
  <BeforeAfterVideo />
  
  <!-- Plans & Documents (EPC, Plot) -->
  <PropertyDocs />
  
  <!-- Floor Plans -->
  <Floorplans />
  
  <!-- Location -->
  <MapEmbed />
  
  <!-- Location: Nearby & Lifestyle Grid -->
  <LocationExtras />
  
  <!-- Book a Viewing Section -->
  <section id="contact" class="p-4 max-w-lg mx-auto text-center">
    <h2 class="text-2xl font-semibold mb-4">Book a Viewing</h2>
    <p class="text-gray-600 mb-6">Schedule your viewing at a time that works for you.</p>
    <a href={site.bookingUrl} target="_blank" rel="noopener" class="inline-block bg-blue-600 text-white px-8 py-3 rounded-lg hover:bg-blue-700 focus:outline-none focus-visible:ring focus-visible:ring-blue-300 transition-colors">
      Book a Viewing
    </a>
  </section>
  
  <!-- FAQ -->
  <FAQ items={faqMd?.frontmatter?.questions || []} />
  
</BaseLayout>
